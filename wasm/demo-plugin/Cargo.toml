[package]
name = "demo-plugin"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
wasm-minimal-protocol = "0.1"
serde_json = "1"

[profile.release]
lto = true
opt-level = 'z'
panic = 'abort'
strip = true
